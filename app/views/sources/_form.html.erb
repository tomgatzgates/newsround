<%= form_for(source) do |f| %>
  <% if source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(source.errors.count, "error") %> prohibited this source from being saved:</h2>

      <ul>
      <% source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :url %>
  <%= f.text_field :url %>

  <%= f.label :political_stance %>
  <%= f.text_area :political_stance %>

  <%= f.label :publication %>
  <%= f.select :publication, options_from_collection_for_select(Publication.all, :id, :name) %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
